#include<stdio.h>
#include<stdlib.h>
#include<string.h>


struct s
{int a;
struct s *next;
struct s *prev;
};

main()
{
	
	struct s *list,*temp,*head;
	int i;
	
	head = (struct s *)malloc(sizeof(struct s));
	head->a = 1;
	
	head->prev = NULL;
	head->next = NULL;
	
	for (i = 0;i<5;i++)
	{
		temp = 
		
		
		
		
	}
	
	
	
	
}